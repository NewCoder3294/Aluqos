import { describe, it } from "vitest";

describe("vault", () => {
  // TODO(task-4): implement once @/src/oauth/vault lands. Convert to it() with body.
  it.todo("stores and reads a secret round-trip");
});
