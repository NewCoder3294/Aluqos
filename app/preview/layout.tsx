// Throwaway preview layout — used to compare three Phase 1 content-card
// designs side by side. Does not affect the real /onboarding flow.
export default function PreviewLayout({ children }: { children: React.ReactNode }) {
  return children;
}
